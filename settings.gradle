pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "WeatherForecastApp"
include ':app'
include(":forecast")
include(":forecast:forecast_data")
include(":forecast:forecast_domain")
include(":forecast:forecast_presentation")
